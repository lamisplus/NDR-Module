<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <property name="autoIncrement" value="true"/>

    <changeSet id="2022221-001" author="amos-data-fi">
        <sql>
            CREATE TABLE hiv_enrollment
            (
                id                        BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
                created_date              TIMESTAMP WITHOUT TIME ZONE,
                created_by                VARCHAR(255),
                last_modified_date        TIMESTAMP WITHOUT TIME ZONE,
                last_modified_by          VARCHAR(255),
                unique_id                 VARCHAR(255),
                entry_point_id            BIGINT,
                target_group_id           BIGINT,
                date_confirmed_hiv        date,
                date_enrolled_pmtct       date,
                source_of_referrer_id     BIGINT,
                time_hiv_diagnosis        date,
                pregnant                  BOOLEAN,
                breastfeeding             BOOLEAN,
                date_of_registration      date,
                status_at_registration_id BIGINT,
                enrollment_setting_id     BIGINT,
                date_started              date,
                send_message              BOOLEAN,
                person_id                 BIGINT,
                facility_id                 BIGINT,
                uuid                      VARCHAR(255),
                archived                  INTEGER,
                facility_name             VARCHAR(255),
                ovc_number                VARCHAR(255),
                date_of_lpm               date,
                pregnancy_status_id       BIGINT,
                tb_status_id              BIGINT,
                CONSTRAINT pk_hiv_enrollment PRIMARY KEY (id)
            );

            ALTER TABLE hiv_enrollment
                ADD CONSTRAINT FK_HIV_ENROLLMENT_ON_PERSON FOREIGN KEY (person_id) REFERENCES person (id);
        </sql>
    </changeSet>

    <changeSet id="20220510-002" author="amos-data-fi">
        <sql>
            CREATE TABLE art
            (
                id                   BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
                created_date         TIMESTAMP WITHOUT TIME ZONE,
                created_by           VARCHAR(255),
                last_modified_date   TIMESTAMP WITHOUT TIME ZONE,
                last_modified_by     VARCHAR(255),
                facility_id          BIGINT,
                art_start_date       date,
                cd_4                 VARCHAR(255),
                cd_4_percentage      BIGINT,
                is_commencement      BOOLEAN,
                functional_status_id BIGINT,
                clinical_stage_id    BIGINT,
                clinical_note        VARCHAR(255),
                uuid                 VARCHAR(255)                            NOT NULL,
                hiv_enrollment_id    BIGINT                                  NOT NULL,
                regimen_id           BIGINT,
                regimen_type_id      BIGINT,
                art_status           BIGINT                                  NOT NULL,
                archived             INTEGER,
                vital_sign_id        BIGINT,
                who_staging_id       BIGINT,
                CONSTRAINT pk_art PRIMARY KEY (id)
            );
            ALTER TABLE art
                ADD CONSTRAINT uc_art_uuid UNIQUE (uuid);

            ALTER TABLE art
                ADD CONSTRAINT FK_ART_ON_HIV_ENROLLMENT FOREIGN KEY (hiv_enrollment_id) REFERENCES hiv_enrollment (id);

            ALTER TABLE art
                ADD CONSTRAINT FK_ART_ON_VITALSIGN FOREIGN KEY (vital_sign_id) REFERENCES vital_sign (id);
        </sql>
    </changeSet>
</databaseChangeLog>
